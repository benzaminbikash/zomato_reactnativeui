import { Dimensions } from "react-native";

const { width, height } = Dimensions.get("window");

export const COLORS = {
  primary: "#DC582C",
  secondary: "#20A9E7",
  pink: "#F091B1",
};
export const SIZE = {
  WIDTH: width,
  HEIGHT: height,
};
